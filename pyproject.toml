[project]
name = "lecture-digest"
version = "0.1.0"
description = "Compile Wispr transcripts, screen recordings, and slide PDFs into study context"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "imagehash>=4.3.2",
    "numpy>=2.5.2",
    "pillow>=12.3.0",
    "pypdf>=5.0.0",
]
